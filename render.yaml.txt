services:
  - type: web
    name: canvas-exporter
    runtime: docker
    plan: free
    envVars:
      - key: ALLOWED_ORIGIN
        value: https://your-frontend-domain.example   # set to your site; or "*" while testing
    healthCheckPath: /docs
